{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Sid": "IAMPassRole",
            "Effect": "Allow",
            "Action": [
                "iam:PassRole"
            ],
            "Resource": ${jsonencode(iam_role_arn)}
        }
    ]
}
