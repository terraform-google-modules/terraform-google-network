apiVersion: v1
kind: Secret
metadata:
  name: monitoring-grafana-admin-auth
  annotations:
    avp.kubernetes.io/path: "{{ .Values.vaultBasePath }}/global/grafana"
type: Opaque
stringData:
  admin-user: <admin-user>
  admin-password: <admin-password>
  